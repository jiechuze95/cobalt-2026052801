# cobalt Web 前端环境变量配置
# 复制本文件为 .env 并修改为实际值：cp .env.example .env

# [必需] API 后端地址（指向 cobalt-api 服务）
WEB_DEFAULT_API=http://localhost:9000/

# 站点域名（可选，用于生成 sitemap）
# WEB_HOST=localhost:5173

# Plausible 统计域名（可选）
# WEB_PLAUSIBLE_HOST=your-plausible-instance.example.com
